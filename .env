# IP address of this PXE server on the network
SERVER_IP=<YOUR_SERVER_IP>

# Network interface to bind dnsmasq to (e.g. eth0, ens3, enp3s0)
INTERFACE=eth0

# Network address for proxy DHCP (no prefix length — dnsmasq detects subnet automatically)
DHCP_NETWORK=192.168.1.0
